train:
  data: data/pcb_yolo/data.yaml  # required
  model: artifacts/yolo/yolo26n.pt  # required
  epochs: 50  # required
  imgsz: 640  # required
  batch: 16  # required
  device: null
  project: runs/exp05  # required
  name: yolo_finetune01 # required
  exist_ok: true
  freeze: 0
  mosaic: 0.0

mlflow:
  tracking_uri: databricks
  experiment_name: /Shared/Experiments/ml-workflow-object-detection-example/exp05_train_yolo
  run_name: yolo_finetune01
  model_name: study.object_detection.yolo01 # {catalog}.{schema}.{model} format