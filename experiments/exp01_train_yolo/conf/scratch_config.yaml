train:
  data: ../../data/pcb_yolo/data.yaml  # required
  model: yolo26n.yaml  # .pt 가 아닌 .yaml 로 기존 학습된 weight은 학습에 사용하지 않음
  epochs: 50  # required
  save_period: 10
  imgsz: 640  # required
  batch: 16  # required
  lr0: 0.0025
  warmup_epochs: 5
  device: null
  project: runs/exp01_train_yolo  # required
  name: yolo_scratch # required
  exist_ok: true
  freeze: 0
  mosaic: 0.0

mlflow:
  tracking_uri: databricks
  experiment_name: /Shared/Experiments/exp05_train_yolo
  run_name: yolo_scratch
